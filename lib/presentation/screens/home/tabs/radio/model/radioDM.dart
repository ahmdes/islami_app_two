import 'package:flutter/material.dart';

class RadioDM {
  String backGroundImage, name;
  IconData playOrStop, volumeOfSound;
  RadioDM(
      {required this.name,
      required this.backGroundImage,
      required this.playOrStop,
      required this.volumeOfSound});
}
